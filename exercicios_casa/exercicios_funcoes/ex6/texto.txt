Escreva uma função, usando callback, que receba 3 argumentos: uma string, um número de repetições, e um callback que será repetido. A função, chamada repeat, deve passar a string para o callback como argumento e repetir este callback X vezes, com o resultado da repetição anterior como argumento. Insira o link da sua implementação no repl.it.